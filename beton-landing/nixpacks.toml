[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "rm -rf node_modules/.cache 2>/dev/null || true",
  "npm ci --prefer-offline --no-audit"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "node server.js"

